{
  "firestore": {
    "rules": "firestore.rules"
  },
  "hosting": {
    "public": ".",
    "ignore": ["firebase.json", "firestore.rules", ".git/**"],
    "rewrites": [
      { "source": "**", "destination": "/index.html" }
    ]
  }
}
